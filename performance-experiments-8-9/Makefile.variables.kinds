KINDS :=

SH_KINDS := \
	nested_lambda_no_name \
	nested_lambda_same_name \
	nested_lambda_different_name

SPECIAL_KINDS := \
	name_resolution

ALL_VKINDS := $(KINDS) $(SPECIAL_KINDS)
ALL_KINDS := $(ALL_VKINDS) $(SH_KINDS)
